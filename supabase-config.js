// === SUPABASE CONFIG ===
// Fill these in once your Supabase project is created:
// Project → Settings → API → Project URL / anon public key.
// Until both are filled in, auth.html runs in preview mode (no real
// accounts are created, but the full UI/UX flow works end to end).

window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
